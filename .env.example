# Database configuration for The Glam Room
# Copy this file to .env and replace values for your local environment.
# The current project connection uses these same default values in config/database.php.

DB_HOST=localhost
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=glam_room
